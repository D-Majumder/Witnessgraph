# Copy to .env.local (Vite auto-loads it, and it's gitignored) to point
# the UI at a witnessgraph-api server bound to a different host/port than
# the default.
VITE_API_BASE_URL=http://127.0.0.1:8420
